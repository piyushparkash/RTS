#ifndef SCHEDULE_H
#define SCHEDULE_H
#include "Process.h"

class Schedule
{
    public:
        Schedule();
        virtual ~Schedule();

        // Variable to store all the process classes
    protected:
    private:
};

#endif // SCHEDULE_H
